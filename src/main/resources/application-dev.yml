spring:
  datasource:
    url: jdbc:mysql://192.168.99.100:3306/sfg_dev
    username: sfg_dev_user
    password: pawel
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: true
    properties:
      javax:
        persistance:
          schema-generation:
            create-source: metadata
            scripts:
              action: create
              create-targer: database-create.sql
